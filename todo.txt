할 일
0. 아마 각 숫자에 맞는 파일들 만들어보기
1. 깃헙 호스팅러너에서 스프링 빌드해보기(완료)
2. 도커파일에서 빌드(멀티스테이지 빌드)로(완료)
3. 깃헙 호스팅러너에서 캐시 사용해보기
4. GitHub Actions 전체 실행 시간 비교(캐시 있을 때와 없을 때)
	- 도커파일로 빌드와 캐시 적용 비교(maven 캐시 사용)
	- 도커멀티스테이지로 빌드와 캐시 적용 비교
	- actions/cache 직접 사용하여 비교

5. 도커헙으로 이미지 올려보기(도커헙 id, pw빼서 환경변수로 설정해보기)
6. 컨테이너 실행 테스트(도커헙에 방금 올린 이미지를 pull, docker run으로 정상 확인. Health Check 확인. CI에서 Smoke Test 수행. 


환경변수로 빼보기
AWS ECR 연동해보기(ecr에 캐시를 저장)
